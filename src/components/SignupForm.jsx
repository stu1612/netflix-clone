// npm
import { useState } from "react";

export default function SignupForm({ emailState, passwordState }) {
  return (
    <div className="form_wrapper">
      <input type="email" />
    </div>
  );
}
